server:
  applicationConnectors:
    - type: http
      port: ${MARQUEZ_PORT:-9000}
  adminConnectors:
    - type: http
      port: ${MARQUEZ_ADMIN_PORT:-8081}

db:
  driverClass: org.postgresql.Driver
  url: jdbc:postgresql://db:5432/marquez
  user: marquez
  password: marquez
